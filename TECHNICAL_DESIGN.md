# 肉夹馍传奇 - 技术方案设计

## 1. 项目概述

**项目名称**: 肉夹馍传奇 (Roujiamo Legend)
**项目类型**: 2D 模拟经营游戏
**核心玩法**: 类似"沙威玛传奇"的快节奏食物制作与销售游戏

### 1.1 游戏核心循环
```
顾客到达 → 制作肉夹馍 → 完成订单 → 获得金币 → 升级设备
```

### 1.2 制作流程
1. **烤肉** - 旋转烤肉架，割取肉片
2. **准备馍** - 烤制白吉馍
3. **夹肉** - 将肉放入馍中
4. **上菜** - 将成品递给顾客

---

## 2. 技术栈选择

### 2.1 游戏框架对比

| 方案 | UI系统 | 动画系统 | 粒子系统 | 场景管理 | 音频 | 纯代码友好度 |
|------|--------|----------|----------|----------|------|--------------|
| **SDL2** (纯) | 需自建 | 需自建 | 需自建 | 需自建 | SDL_mixer | ⭐⭐⭐⭐⭐ |
| **Raylib** | 内置 | 内置 | 需自建 | 无 | 内置 | ⭐⭐⭐⭐⭐ |
| **SFML** | 需结合 | 需自建 | 需自建 | 需自建 | 内置 | ⭐⭐⭐⭐ |
| **Godot (C++)** | 内置 | 内置 | 内置 | 内置 | 内置 | ⭐⭐⭐ |
| **Cocos2d-x** | 内置 | 内置 | 内置 | 内置 | 内置 | ⭐⭐⭐ |

> **关于"纯编码"**: 以上所有方案都支持纯代码开发，不需要使用任何可视化编辑器

### 2.2 推荐方案

| 使用场景 | 推荐方案 | 理由 |
|----------|----------|------|
| 想深入学习底层 | SDL2 | 最底层，完全可控 |
| **追求效率 + 纯代码** | **Raylib** | 内置 UI/动画，即时模式，代码即配置 |
| 需要完整引擎功能 | Godot / Cocos2d-x | 功能最全，但有框架复杂度 |

### 2.2 音频
- **SDL2_mixer**: 支持 WAV、MP3、OGG

### 2.3 物理
- **自定义简单物理**: 游戏主要使用碰撞检测和简单动画，不需要复杂物理引擎

### 2.4 构建系统
- **CMake**: 跨平台构建
- **vcpkg**: 依赖管理

---

## 3. 架构设计

### 3.1 项目结构

```
RoujiamoLegend/
├── src/
│   ├── main.cpp              # 入口点
│   ├── game/
│   │   ├── Game.cpp/h        # 游戏主循环
│   │   ├── GameState.cpp/h   # 游戏状态管理
│   │   └── SceneManager.cpp/h # 场景管理
│   ├── core/
│   │   ├── Engine.cpp/h      # 引擎核心
│   │   ├── Renderer.cpp/h    # 渲染器
│   │   ├── Input.cpp/h       # 输入处理
│   │   ├── Audio.cpp/h       # 音频管理
│   │   ├── Timer.cpp/h       # 定时器
│   │   └── ResourceManager.cpp/h # 资源管理
│   ├── entities/
│   │   ├── Entity.cpp/h      # 实体基类
│   │   ├── Customer.cpp/h    # 顾客
│   │   ├── Chef.cpp/h        # 厨师/玩家角色
│   │   ├── FoodItem.cpp/h    # 食物项目
│   │   └── CustomerQueue.cpp/h # 顾客队列
│   ├── gameobjects/
│   │   ├── GameObject.cpp/h  # 游戏对象基类
│   │   ├── MeatGrill.cpp/h   # 烤肉架
│   │   ├── Oven.cpp/h        # 烤馍炉
│   │   ├── CuttingBoard.cpp/h # 砧板
│   │   ├── Counter.cpp/h     # 柜台
│   │   └── CashRegister.cpp/h # 收银机
│   ├── systems/
│   │   ├── OrderSystem.cpp/h    # 订单系统
│   │   ├── UpgradeSystem.cpp/h  # 升级系统
│   │   ├── CustomerSpawner.cpp/h # 顾客生成
│   │   └── ScoreSystem.cpp/h    # 分数系统
│   ├── ui/
│   │   ├── UIElement.cpp/h   # UI 元素基类
│   │   ├── Button.cpp/h      # 按钮
│   │   ├── ProgressBar.cpp/h # 进度条
│   │   ├── Label.cpp/h       # 文本标签
│   │   ├── Panel.cpp/h       # 面板
│   │   └── UIManager.cpp/h   # UI 管理器
│   ├── animation/
│   │   ├── Animation.cpp/h   # 动画基类
│   │   ├── SpriteAnimation.cpp/h # 精灵动画
│   │   └── BoneAnimation.cpp/h   # 骨骼动画(可选)
│   ├── data/
│   │   ├── SaveData.cpp/h    # 存档数据
│   │   ├── GameConfig.cpp/h  # 游戏配置
│   │   └── UpgradeData.cpp/h # 升级数据
│   └── utils/
│       ├── Logger.cpp/h      # 日志
│       ├── MathUtils.cpp/h   # 数学工具
│       ├── StringUtils.cpp/h # 字符串工具
│       └── FileUtils.cpp/h   # 文件工具
├── assets/
│   ├── textures/             # 纹理图片
│   │   ├── background/
│   │   ├── characters/
│   │   ├── food/
│   │   ├── UI/
│   │   └── effects/
│   ├── audio/
│   │   ├── bgm/
│   │   └── sfx/
│   ├── fonts/
│   └── animations/           # 动画帧序列
├── data/
│   └── game.json             # 游戏数据配置
├── CMakeLists.txt
├── vcpkg.json
└── README.md
```

### 3.2 核心类图

```
┌─────────────────────────────────────────────────────────────────┐
│                         Engine                                  │
│  - Renderer*  - Input*  - Audio*  - Timer*  - ResourceManager* │
│  - Init() → Run() → Shutdown()                                  │
└─────────────────────┬───────────────────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
┌──────────────┐ ┌──────────┐ ┌──────────┐
│   GameState  │ │  Scene   │ │  Systems │
│  - Playing   │ │ - Main   │ │ - Order  │
│  - Paused    │ │ - Shop   │ │ - Upgrade│
│  - Menu      │ │ - Game   │ │ - Spawn  │
└──────────────┘ └──────────┘ └──────────┘
```

### 3.3 状态机设计

```
┌─────────┐    start    ┌─────────┐
│  MENU   │ ──────────► │ PLAYING │
└─────────┘             └────┬────┘
      ▲                      │
      │    pause    ┌────────┴────────┐
      └──────────── │      PAUSED     │
                   └──────────────────┘
                        │ game over
                        ▼
                   ┌──────────┐
                   │  GAMEOVER│
                   └──────────┘
```

---

## 4. 游戏系统设计

### 4.1 顾客系统

```cpp
struct Customer {
    std::string name;           // 顾客名称
    CustomerType type;          // 顾客类型(普通/VIP/急躁等)
    int patience;               // 耐心值(倒计时)
    Order order;                // 点的订单
    float spawnPosition;        // 生成位置
    CustomerState state;        // 等待/点餐/等待食物/离开
};
```

**顾客类型**:
| 类型 | 耐心时间 | 奖励倍数 | 特点 |
|------|----------|----------|------|
| 普通 | 30秒 | 1x | 正常 |
| 着急 | 15秒 | 1.5x | 速度快 |
| VIP | 45秒 | 2x | 点的多 |
| 老板 | 60秒 | 3x | 随机小费 |

### 4.2 订单系统

```cpp
struct Order {
    int roujiamoCount;      // 肉夹馍数量
    bool wantBeverage;      // 是否要饮料
    float tip;              // 小费
    int requiredMeat;       // 需要的肉量
    bool requiresSpicy;     // 是否要辣
};
```

### 4.3 制作系统

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│ 烤肉架  │───►│  砧板   │───►│  组装   │───►│  柜台  │
│ (烤肉)  │    │ (切肉)  │    │ (夹馍)  │    │ (交付) │
└─────────┘    └─────────┘    └─────────┘    └─────────┘
     │              │              │              │
     ▼              ▼              ▼              ▼
  进度条        切割动画       完成判定      订单完成
```

**每个工位**:
- 有独立的进度条
- 有状态(空闲/工作中/完成)
- 有升级路径

### 4.4 升级系统

| 升级项 | 等级 | 效果 |
|--------|------|------|
| 烤肉速度 | 1-5 | 烤肉时间 -20%/级 |
| 烤馍速度 | 1-5 | 烤馍时间 -20%/级 |
| 最大顾客 | 1-3 | 同时服务顾客 +2/级 |
| 耐心提升 | 1-3 | 顾客耐心 +10秒/级 |
| 小费加成 | 1-5 | 小费 +25%/级 |
| 自动切肉 | 1-3 | 切肉自动化 |

---

## 5. 渲染系统设计

### 5.1 渲染器架构

```cpp
class Renderer {
public:
    void Init(WindowConfig config);
    void BeginFrame();
    void EndFrame();
    
    // 2D 渲染
    void DrawTexture(TextureID id, const Vec2& pos, ...);
    void DrawSprite(const Sprite& sprite);
    void DrawText(const std::string& text, const Vec2& pos, ...);
    void DrawRect(const Rect& rect, Color color);
    void DrawCircle(const Vec2& center, float radius, Color color);
    
    // 特效
    void SetShader(ShaderID id);
    void ApplyPostProcess(PostProcessType type);
};
```

### 5.2 视图层次

```
Background Layer (背景层)
    │
    ├── Static Background (静态背景)
    │
Game Layer (游戏层)
    │
    ├── Furniture (家具/设备)
    │
    ├── Interactive Objects (交互对象)
    │
    ├── Characters (角色)
    │     ├── Chef (厨师)
    │     └── Customers (顾客)
    │
    └── Food Items (食物)

UI Layer (UI层)
    │
    ├── HUD (抬头显示)
    │     ├── Score Display
    │     ├── Money Display
    │     └── Timer
    │
    ├── Order Tickets (订单票据)
    │
    └── Upgrade Menu (升级菜单)
```

### 5.3 坐标系统

- 使用像素坐标，基准分辨率 1280x720
- 支持缩放适配不同屏幕
- UI 元素使用屏幕相对坐标

---

## 6. 输入系统设计

### 6.1 输入映射

```cpp
enum class InputAction {
    MoveLeft,
    MoveRight,
    Interact,        // 交互(点击工作台)
    PickUp,          // 拾取
    Drop,            // 放下
    Pause,           // 暂停
    DebugSkip        // 调试跳过(开发用)
};

struct InputConfig {
    KeyCode moveLeft = KEY_A;
    KeyCode moveRight = KEY_D;
    KeyCode interact = KEY_SPACE;
    KeyCode pickUp = KEY_E;
    KeyCode drop = KEY_Q;
    KeyCode pause = KEY_ESCAPE;
};
```

### 6.2 鼠标/触摸支持

- 鼠标点击工作台触发交互
- 支持触屏设备(移动端)
- 点击位置映射到游戏坐标

---

## 7. 数据持久化

### 7.1 存档结构 (JSON)

```json
{
    "version": "1.0.0",
    "player": {
        "money": 1500,
        "totalSales": 42,
        "bestTime": 180
    },
    "upgrades": {
        "grillSpeed": 3,
        "ovenSpeed": 2,
        "maxCustomers": 2,
        "patience": 1,
        "tipBonus": 0,
        "autoSlice": 0
    },
    "achievements": [
        "first_sale",
        "ten_sales",
        "upgrade_master"
    ]
}
```

### 7.2 存储位置
- Windows: `%APPDATA%/RoujiamoLegend/save.json`
- macOS: `~/Library/Application Support/RoujiamoLegend/save.json`
- Linux: `~/.local/share/RoujiamoLegend/save.json`

---

## 8. 资源格式

### 8.1 纹理
- 格式: PNG (带透明通道)
- 尺寸: 2 的幂次方 (如 256x256, 512x512)
- 打包: 使用图集(Texture Atlas)

### 8.2 音频
- BGM: OGG 或 MP3
- SFX: WAV (短促音效)

### 8.3 字体
- 主字体: 思源黑体 或类似开源字体
- 字号: 12, 16, 24, 32, 48

---

## 9. 平台支持

| 平台 | 状态 | 说明 |
|------|------|------|
| Windows | 必选 | 主要开发平台 |
| macOS | 可选 | 使用 Cocoa 窗口 |
| Linux | 可选 | X11/Wayland |
| Android | 可选 | 需要 SDL2_android |
| iOS | 可选 | 需要额外配置 |

---

## 10. 开发计划

### Phase 1: 基础框架
- [x] 项目搭建 (CMake + SDL2)
- [x] 渲染系统
- [x] 输入系统
- [x] 基础 UI 系统

### Phase 2: 核心玩法
- [ ] 玩家移动与交互
- [ ] 烤肉架系统
- [ ] 烤馍炉系统
- [ ] 食物组装系统

### Phase 3: 顾客系统
- [ ] 顾客生成与队列
- [ ] 订单系统
- [ ] 耐心机制
- [ ] 收益计算

### Phase 4: 完善与优化
- [ ] 升级系统
- [ ] 音效与音乐
- [ ] 存档系统
- [ ] 动画与特效

---

## 11. 依赖项

```json
{
    "dependencies": [
        "sdl2",
        "sdl2-image",
        "sdl2-mixer",
        "sdl2-ttf",
        "nlohmann-json"
    ]
}
```

---

## 12. 参考资源

- [SDL2 文档](https://wiki.libsdl.org/)
- [SDL2 Game Tutorial](https://gigi.nullneuron.net/gigilabs/sdl2-game-tutorial/)
- 沙威玛传奇游戏截图与玩法分析

---

## 13. 团队分工方案 (两人开发)

### 分工原则

```
┌─────────────────────────────────────────────────────────────────┐
│                     角色分配示意图                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│    程序员 A (引擎/图形)          程序员 B (游戏逻辑)            │
│                                                                 │
│  ┌──────────────────┐         ┌──────────────────┐            │
│  │   渲染系统       │         │   游戏逻辑       │            │
│  │   动画系统       │◄───────►│   顾客AI         │            │
│  │   特效系统       │  数据   │   订单系统       │            │
│  │   UI渲染         │  接口   │   升级系统       │            │
│  └──────────────────┘         └──────────────────┘            │
│           │                            │                       │
│           ▼                            ▼                       │
│  ┌──────────────────────────────────────────────┐             │
│  │              共享模块 (两人协作)               │             │
│  │  • 资源加载  • 存档系统  • 输入系统  • 配置   │             │
│  └──────────────────────────────────────────────┘             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

### 13.1 程序员 A: 引擎与图形 (Engine & Graphics)

**职责**: 负责游戏底层基础设施和视觉表现

#### 核心模块
| 模块 | 描述 | 文件 |
|------|------|------|
| **Engine** | 游戏主循环、初始化、退出 | Engine.cpp/h |
| **Renderer** | 2D渲染、纹理管理、绘制 | Renderer.cpp/h |
| **ResourceManager** | 资源加载、缓存、释放 | ResourceManager.cpp/h |
| **Input** | 键盘/鼠标/触摸输入 | Input.cpp/h |
| **Audio** | 背景音乐、音效播放 | Audio.cpp/h |
| **Timer** | 时间管理、帧率控制 | Timer.cpp/h |

#### 视觉表现
| 模块 | 描述 | 文件 |
|------|------|------|
| **Animation** | 精灵动画系统 | Animation.cpp/h, SpriteAnimation.cpp/h |
| **ParticleSystem** | 粒子特效(烤肉烟尘、收钱特效) | ParticleSystem.cpp/h |
| **UIStyle** | UI视觉样式、主题 | UIStyle.cpp/h |

#### UI 基础设施
| 模块 | 描述 | 文件 |
|------|------|------|
| **UIElement** | UI基类、事件系统 | UIElement.cpp/h |
| **UIManager** | UI布局、渲染顺序 | UIManager.cpp/h |
| **Widget** | 基础控件(按钮、进度条) | Button.cpp/h, ProgressBar.cpp/h |

#### 交付物
- ✅ 可运行的空白窗口
- ✅ 纹理加载与渲染
- ✅ 基础 UI 组件
- ✅ 输入响应系统

#### 依赖项目
- 程序员 B 的交付物: `GameObject` 基类、事件回调接口

---

### 13.2 程序员 B: 游戏逻辑 (Gameplay)

**职责**: 负责游戏核心玩法和系统实现

#### 核心游戏逻辑
| 模块 | 描述 | 文件 |
|------|------|------|
| **Game** | 游戏主状态管理 | Game.cpp/h |
| **GameState** | 状态机(菜单/游戏中/暂停) | GameState.cpp/h |
| **SceneManager** | 场景切换管理 | SceneManager.cpp/h |

#### 实体系统
| 模块 | 描述 | 文件 |
|------|------|------|
| **Entity** | 实体基类、移动、碰撞 | Entity.cpp/h |
| **Chef** | 玩家角色、控制、动画状态 | Chef.cpp/h |
| **Customer** | 顾客AI、行为树、耐心 | Customer.cpp/h |
| **CustomerQueue** | 顾客队列、生成逻辑 | CustomerQueue.cpp/h |
| **FoodItem** | 食物状态(烤肉、馍、成品) | FoodItem.cpp/h |

#### 游戏对象 (工位)
| 模块 | 描述 | 文件 |
|------|------|------|
| **GameObject** | 游戏对象基类 | GameObject.cpp/h |
| **MeatGrill** | 烤肉架、烤肉进度 | MeatGrill.cpp/h |
| **Oven** | 烤馍炉、烤馍进度 | Oven.cpp/h |
| **CuttingBoard** | 砧板、切肉操作 | CuttingBoard.cpp/h |
| **Counter** | 柜台、交付食物 | Counter.cpp/h |
| **CashRegister** | 收银、收益计算 | CashRegister.cpp/h |

#### 游戏系统
| 模块 | 描述 | 文件 |
|------|------|------|
| **OrderSystem** | 订单生成、匹配、完成 | OrderSystem.cpp/h |
| **UpgradeSystem** | 升级购买、效果应用 | UpgradeSystem.cpp/h |
| **ScoreSystem** | 分数计算、排行榜 | ScoreSystem.cpp/h |
| **CustomerSpawner** | 顾客生成逻辑、波次 | CustomerSpawner.cpp/h |

#### 数据系统
| 模块 | 描述 | 文件 |
|------|------|------|
| **SaveData** | 存档序列化/反序列化 | SaveData.cpp/h |
| **GameConfig** | 游戏配置读取 | GameConfig.cpp/h |
| **UpgradeData** | 升级数据定义 | UpgradeData.cpp/h |

#### 交付物
- ✅ 完整游戏逻辑
- ✅ 顾客行为
- ✅ 订单与制作流程
- ✅ 存档功能

#### 依赖项目
- 程序员 A 的交付物: `Renderer`、`Input`、`UIElement`

---

### 13.3 共享模块 (两人协作)

这些模块需要两人共同讨论接口，一方实现主逻辑，另一方配合：

| 模块 | 主要负责 | 协作方 | 描述 |
|------|----------|--------|------|
| **Logger** | A | B | 日志系统，调试用 |
| **Constants** | 共同 | - | 全局常量、枚举定义 |
| **Events** | 共同 | - | 事件系统(订单完成、金币变化等) |
| **game.json** | B | A | 数值配置 |

---

### 13.4 开发顺序 (关键路径)

```
Week 1-2: 程序员A 搭建框架，程序员B 熟悉需求

Week 3-4: ┌──────────────────────────────────────┐
           │  程序员A: Renderer + Input + Window  │
           │  程序员B: Entity 基类 + 游戏状态      │
           └──────────────────────────────────────┘

Week 5-6: ┌──────────────────────────────────────┐
           │  程序员A: 基础 UI 组件                │
           │  程序员B: Chef 移动 + 工位交互        │
           └──────────────────────────────────────┘

Week 7-8: ┌──────────────────────────────────────┐
           │  程序员A: 动画系统 + 粒子             │
           │  程序员B: 顾客生成 + 订单系统         │
           └──────────────────────────────────────┘

Week 9-10:┌──────────────────────────────────────┐
           │  程序员A: 音效集成 + 优化             │
           │  程序员B: 升级系统 + 存档             │
           └──────────────────────────────────────┘

Week 11:  ┌──────────────────────────────────────┐
           │  程序员A: Bug 修复                   │
           │  程序员B: 平衡性调整                 │
           └──────────────────────────────────────┘

Week 12:  ┌──────────────────────────────────────┐
           │  两人: 打包发布 + 测试               │
           └──────────────────────────────────────┘
```

---

### 13.5 接口规范示例

为确保并行开发，双方需要预先定义接口：

```cpp
// ===== 程序员B 定义，程序员A 使用 =====

// 事件系统 (Shared)
enum class GameEvent {
    OrderCompleted,
    MoneyChanged,
    CustomerArrived,
    CustomerLeft,
    UpgradePurchased
};

using EventCallback = std::function<void(const EventData&)>;
void Subscribe(GameEvent event, EventCallback callback);
void Emit(GameEvent event, const EventData& data);

// ===== 程序员B 定义，程序员A 使用 =====

// UI 回调接口
class IInteractable {
public:
    virtual void OnClick(const Vec2& worldPos) = 0;
    virtual void OnHover() = 0;
    virtual void OnHoverEnd() = 0;
};

// ===== 程序员A 定义，程序员B 使用 =====

// 渲染接口
class IRenderable {
public:
    virtual void Render(Renderer& renderer) = 0;
    virtual void SetPosition(const Vec2& pos) = 0;
    virtual Vec2 GetPosition() const = 0;
};

// 输入事件
class InputManager {
public:
    static bool IsKeyDown(KeyCode key);
    static bool IsKeyPressed(KeyCode key);
    static Vec2 GetMousePosition();
    static bool IsMouseButtonDown(MouseButton btn);
};
```

---

### 13.6 代码审查与合并

```bash
# 分支策略
main                    # 主分支(稳定)
├── engine/graphics     # 程序员A 开发分支
└── gameplay            # 程序员B 开发分支

# 每日流程
1. 每天早上 pull latest main
2. 在自己分支开发
3. 每天下班前 push 自己的分支
4. 每周合并一次到 main (Code Review)
```

---

### 13.7 职责对照表

| 类别 | 程序员 A | 程序员 B |
|------|----------|----------|
| **核心** | 引擎主循环 | 游戏状态机 |
| **渲染** | 所有绘制 | 渲染调用 |
| **输入** | 输入处理 | 绑定到游戏逻辑 |
| **UI** | 组件实现 | 界面逻辑 |
| **音频** | 播放接口 | 触发时机 |
| **实体** | 移动/碰撞渲染 | AI/行为 |
| **工位** | 渲染 | 交互逻辑 |
| **存档** | 文件IO | 数据结构 |
| **配置** | 加载接口 | 数值设计 |
| **调试** | 日志系统 | 游戏内作弊 |

---

### 13.8 常见协作问题与解决方案

| 问题 | 解决方案 |
|------|----------|
| 接口变更频繁 | 使用接口抽象，预先定义头文件 |
| 等待对方代码 | 共同确定接口后，各自用 mock 实现独立开发 |
| 合并冲突 | 每天小合并，避免大规模改动 |
| 资源格式不统一 | 预先定义资源规范文档 |
| 数值不平衡 | 程序员 B 主导，用配置文件调整 |

---

## 14. AI 辅助开发时间估算

> **重要**: 以下估算基于两人同时使用 Claude Code 辅助开发

### 14.1 效率提升因素

| 传统开发 | AI 辅助开发 | 提升比例 |
|----------|-------------|----------|
| 样板代码手写 | 自动生成 | ~80% 时间节省 |
| Bug 调试 | AI 定位 + 修复建议 | ~60% 时间节省 |
| 文档编写 | AI 生成注释和文档 | ~70% 时间节省 |
| 查阅文档 | AI 直接解答 | ~50% 时间节省 |

**综合效率提升: 约 3-5 倍**

---

### 14.2 重新评估后的开发计划

| 阶段 | 工作内容 | 人工估算 | AI 辅助估算 |
|------|----------|----------|-------------|
| **Week 1** | 项目搭建 + 渲染基础 | 3 周 | 3-5 天 |
| **Week 2** | 输入系统 + 基础 UI | 2 周 | 2-3 天 |
| **Week 3** | 实体系统 + 移动 | 2 周 | 2-3 天 |
| **Week 4** | 工位交互系统 | 2 周 | 2-3 天 |
| **Week 5** | 顾客系统 + 订单 | 2 周 | 2-3 天 |
| **Week 6** | 升级 + 存档系统 | 1 周 | 1-2 天 |
| **Week 7** | 动画 + 特效 + 音效 | 1 周 | 1-2 天 |
| **Week 8** | 测试 + 优化 + Bug 修复 | 2 周 | 3-5 天 |

**总计: ~4-5 周** (传统开发约 15 周)

---

### 14.3 AI 协作开发流程

```bash
┌─────────────────────────────────────────────────────────────────┐
│                    每日开发流程 (AI 辅助)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  早上: 10 分钟                                                    │
│  ├── 规划今日任务 (使用 TaskCreate)                              │
│  ├── 明确接口约定 (如果需要与搭档协作)                            │
│                                                                 │
│  白天: 迭代开发 (每 1-2 小时一轮)                                │
│  ├── 编写 prompt 描述需求                                        │
│  ├── Claude Code 生成代码                                        │
│  ├── 本地编译测试                                                │
│  └── 如有 bug → 描述给 AI 修复                                   │
│                                                                 │
│  傍晚: 30 分钟                                                   │
│  ├── 代码 review                                                 │
│  ├── 推送分支                                                    │
│  └── 规划明日任务                                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

### 14.4 Claude Code 使用建议

#### 高效使用技巧

1. **提供完整上下文**
   ```markdown
   我在写一个 SDL2 游戏，请帮我实现 Renderer 类
   - 使用 C++17
   - 需要支持纹理、矩形、文本绘制
   - 参考文件: src/core/Engine.cpp
   ```

2. **分块请求**
   - 不要一次让 AI 生成整个系统
   - 按类/模块拆分，每次 100-300 行

3. **复用已有代码**
   - 告诉 AI 已有类似实现，让它参考风格

4. **Bug 修复**
   ```markdown
   编译报错: undefined reference to `Renderer::DrawTexture'
   文件: src/core/Renderer.cpp:45
   头文件已包含，请检查实现
   ```

#### 推荐的工作模式

| 任务类型 | Claude Code 角色 | 人类角色 |
|----------|------------------|----------|
| 样板代码生成 | 主要执行者 | 审查 + 需求描述 |
| 复杂逻辑设计 | 协作者 | 决策 + 验收 |
| Bug 定位 | 主要执行者 | 验证修复 |
| API 学习 | 主要执行者 | 应用场景说明 |
| 代码重构 | 执行者 | 确认边界 |

---

### 14.5 精简版开发计划

> 适合两人 + Claude Code 的紧凑计划

```
Week 1: 基础架构
├── A: CMake + SDL2 初始化 + 窗口
├── B: 基础类型定义 + Logger + Event
└── 交付: 可运行窗口 + 事件系统

Week 2: 核心系统
├── A: Renderer + Input + Timer
├── B: Game 状态机 + SceneManager
└── 交付: 渲染 + 输入响应

Week 3: 游戏对象
├── A: 基础 UI 组件 (Button, ProgressBar)
├── B: Chef 移动 + 工位基类
└── 交付: 玩家可移动 + 基础交互

Week 4: 制作系统
├── A: 进度条渲染 + 动画系统
├── B: 烤肉/烤馍/切肉/组装逻辑
└── 交付: 完整制作流程可玩

Week 5: 顾客与订单
├── A: 粒子特效 + UI 布局
├── B: 顾客生成 + 订单匹配 + 收益
└── 交付: 游戏核心循环可玩

Week 6: 完善与发布
├── A: 音效 + 存档
├── B: 升级系统 + 平衡调整
└── 交付: 可发布版本
```

**预计总工期: 6 周**

---

### 14.6 引擎对比工作量分析

#### SDL2 (纯手写系统)

| 需要自己实现的模块 | 预估工作量 |
|-------------------|------------|
| Renderer (纹理/图形/文本) | 300-400 行 |
| UI 系统 (Button/Panel/ProgressBar) | 500-600 行 |
| 动画系统 (Sprite/Bone) | 300-400 行 |
| 粒子系统 | 200-300 行 |
| 场景管理器 | 150-200 行 |
| **合计** | **~1500-2000 行** |

#### Raylib (内置系统)

| 使用内置功能 | 预估工作量 |
|-------------|------------|
| 图形渲染 | 0 行 (内置) |
| 基础 UI (raygui) | 0 行 (内置) |
| 字体/文本 | 0 行 (内置) |
| 音频播放 | 0 行 (内置) |
| 2D 绘图工具 | 0 行 (内置) |
| 自定义扩展 | 200-300 行 |
| **合计** | **~200-300 行** |

---

### 14.7 推荐: 使用 Raylib

**Raylib 优势**:

1. **极简设计**: 即时模式，代码即配置，无需编辑器
2. **内置丰富**:
   - `DrawTexture()`, `DrawText()`, `DrawCircle()` 等
   - `RayGUI` 官方 UI 库
   - 音频 `PlaySound()`, `PlayMusic()`
   - 鼠标/键盘/触摸统一 API
3. **纯代码友好**: 所有功能通过函数调用，无需配置文件
4. **编译简单**: 单头文件 + 静态库，CMake 集成简单
5. **文档优秀**: 官网 raylib.com 有丰富的示例

**Raylib 代码示例**:
```cpp
#include "raylib.h"

int main() {
    InitWindow(1280, 720, "肉夹馍传奇");
    SetTargetFPS(60);

    Texture2D chef = LoadTexture("assets/chef.png");
    Vector2 playerPos = {640, 360};

    while (!WindowShouldClose()) {
        // 输入
        if (IsKeyDown(KEY_A)) playerPos.x -= 5;
        if (IsKeyDown(KEY_D)) playerPos.x += 5;

        // 渲染
        BeginDrawing();
        ClearBackground(RAYWHITE);
        DrawTexture(chef, playerPos.x, playerPos.y, WHITE);
        DrawText("肉夹馍传奇", 10, 10, 30, DARKGRAY);
        EndDrawing();
    }

    UnloadTexture(chef);
    CloseWindow();
    return 0;
}
```

**结论**: 使用 Raylib 可以将工作量降低约 **70-80%**，同时保持纯代码开发体验

---

### 14.8 最终推荐

| 目标 | 推荐方案 |
|------|----------|
| 学习底层原理 | SDL2 |
| **快速开发 + 纯代码** | **Raylib** |
| 商业级 2D 游戏 | Cocos2d-x |
| 需要完整编辑器 | Godot |

**本项目推荐: Raylib** (纯代码开发效率最高)

---

## 15. 一周开发方案分析

### 15.1 现实分析

要在一周内完成类似沙威玛传奇的游戏，需要满足：
- ✅ 完整游戏循环 (制作 → 销售 → 升级)
- ✅ 多个可交互工位 (烤肉、烤馍、切肉、组装)
- ✅ 顾客 AI 和排队系统
- ✅ UI (分数、金币、升级菜单)
- ✅ 存档功能

**核心矛盾**: 游戏逻辑部分（顾客行为、工位交互、订单系统）无论如何都需要开发，这是逃不掉的。

### 15.2 方案对比

| 方案 | 优点 | 缺点 | 一周可行性 |
|------|------|------|------------|
| **SDL2** | 灵活 | 大量样板代码 | ❌ 难 |
| **Raylib** | 简化渲染 | 逻辑仍需开发 | ⚠️ 紧 |
| **Cocos2d-x** | 功能全 | 框架重、学习曲线 | ⚠️ 紧 |
| **Godot (C++)** | 功能全 | 需学GDScript | ❌ |
| **Godot (纯项目)** | 一周可完成 | 需用编辑器 | ❌ 不符合需求 |

### 15.3 关键洞察

```
游戏开发工作量 = 渲染系统 + UI系统 + 游戏逻辑

渲染系统: Raylib 内置 ✓
UI系统:    RayGUI 内置 ✓
游戏逻辑:  无论如何都需要开发 ❌
```

**结论**: 不存在"不需要写游戏逻辑"的方案，任何游戏都需要实现核心玩法。

### 15.4 可行的一周方案 (Raylib)

| 天 | 任务 | 交付物 |
|----|------|--------|
| **Day 1** | 项目搭建 + 渲染基础 + 玩家移动 | 窗口 + 可移动角色 |
| **Day 2** | 工位系统 (烤肉/烤馍/切肉/组装) | 可点击交互 |
| **Day 3** | 食物状态机 + 制作流程 | 完整制作链 |
| **Day 4** | 顾客生成 + 队列 + 订单 | 顾客出现到离开 |
| **Day 5** | 收益计算 + 存档 | 可赚钱 |
| **Day 6** | 升级系统 + UI 美化 | 可购买升级 |
| **Day 7** | Bug 修复 + 测试 + 打包 | 可发布版本 |

**可行性**: 紧张但可行，需要：
- 每天高效工作 6-8 小时
- Claude Code 辅助编码
- 双方紧密配合
- 接受部分功能简化

### 15.5 简化建议 (如果要确保一周完成)

可以接受以下简化：
1. **图形**: 使用色块代替图片素材
2. **音效**: 跳过或使用占位符
3. **存档**: 简化为单文件 JSON
4. **顾客**: 1-2 种类型即可
5. **升级**: 3-4 项核心升级

---

## 16. 为什么 Godot 编辑器能更快？

### 16.1 图形库 vs 游戏引擎的本质区别

```
┌─────────────────────────────────────────────────────────────────┐
│                    图形库 (SDL2/Raylib)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  你需要自己实现:                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  场景管理   │  │  动画系统   │  │  碰撞检测   │             │
│  │  (菜单→游戏)│  │  (播放/切换)│  │  (点击判定) │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  UI 布局    │  │  资源加载   │  │  物理模拟   │             │
│  │  (按钮位置) │  │  (纹理缓存) │  │  (移动/反弹)│             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
│  = 大量基础设施代码                                              │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    游戏引擎 (Godot)                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  编辑器内置:                                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  场景系统   │  │  动画编辑器 │  │  物理引擎   │             │
│  │  (节点树)   │  │  (可视化)   │  │  (内置)     │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  UI 编辑器  │  │  资源管理   │  │  调试工具   │             │
│  │  (可视化)   │  │  (导入)     │  │  (实时)     │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
│  你只需专注: 业务逻辑 (游戏玩法)                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 16.2 具体对比

| 需要实现的内容 | SDL2/Raylib (纯代码) | Godot (编辑器) |
|---------------|---------------------|----------------|
| **场景切换** | 100行代码 (手动管理) | 拖拽节点 → 完成 |
| **角色动画** | 200行代码 (帧切换) | 动画状态机 → 完成 |
| **碰撞检测** | 150行代码 (矩形碰撞) | Area2D 节点 → 完成 |
| **UI 按钮** | 300行代码 (布局+事件) | Control 节点 → 完成 |
| **资源加载** | 200行代码 (加载+缓存) | 自动导入 → 完成 |
| **调试** | print 大法 | 内置调试器 |

### 16.3 但有一个重要前提

```
⚠️  Godot 官方推荐使用 GDScript，不是 C++

GDScript 优点:
  • 类似 Python，易学
  • 无需编译，修改即生效
  • 与 Godot 深度集成
  • 开发效率极高

C++ 的问题:
  • 需要编译，迭代慢
  • 很多 Godot 功能需要用 GDScript 包装
  • 官方文档主要针对 GDScript
  • 反而降低了效率
```

### 16.4 真相大白了

**所以我的建议有误！**

如果你说"纯 C++ 实现"，那么：
- Godot 编辑器**不能**帮你省时间
- 因为 Godot + C++ = 反而更麻烦

**真正的一周方案**:

| 语言 | 方案 | 一周可行性 |
|------|------|------------|
| **GDScript** | Godot 编辑器 | ✅ 完全可行 |
| **C++** | Raylib | ⚠️ 紧张可行 |
| **C++** | SDL2 | ❌ 不可能 |

### 16.5 如果坚持要 C++

那么 **Raylib** 已经是最优选择，因为：
- 内置渲染、UI(RayGUI)、音频
- 比 SDL2 少写 ~1000+ 行样板代码
- 配合 Claude Code 可以一周冲刺

---

### 16.6 最终结论

| 你的需求 | 推荐方案 |
|----------|----------|
| 纯 C++ + 一周 | **Raylib** (需要高度简化) |
| 纯 C++ + 两周 | **Raylib** (完整功能) |
| 接受 GDScript + 一周 | **Godot** (完整功能) |

**你想怎么选？**

---

## 17. Raylib + C++ 一周极限精简方案

### 17.1 沙威玛传奇完整功能清单

| 功能模块 | 完整版 | 可简化程度 |
|----------|--------|------------|
| **工位数量** | 4个 (烤肉、烤馍、切肉、组装) | ⚠️ 可合并 |
| **顾客类型** | 4种 (普通/着急/VIP/老板) | ⭐ 简化为1种 |
| **顾客行为** | 排队、点餐、等待、发火、离开 | ⭐ 简化流程 |
| **订单系统** | 随机多样订单 | ⭐ 固定1种订单 |
| **升级系统** | 6种升级，每种5级 | ⭐ 简化为2-3种 |
| **存档系统** | 完整JSON存档 | ❌ 可跳过 |
| **音效/音乐** | BGM + 多种SFX | ❌ 直接跳过 |
| **图形素材** | 精美图片 | ❌ 用色块代替 |
| **动画效果** | 流畅动画 | ⭐ 简化或跳过 |
| **UI 系统** | 精美界面 | ⚠️ 基础即可 |

### 17.2 每日任务细化 (一周)

```
┌─────────────────────────────────────────────────────────────────┐
│                         Day 1: 基础框架                         │
├─────────────────────────────────────────────────────────────────┤
│  ✓ 项目搭建 (CMake + Raylib)                                    │
│  ✓ 游戏窗口 + 主循环                                            │
│  ✓ 基础输入处理 (键盘 + 鼠标)                                   │
│  ✓ 玩家角色显示 (色块) + 移动                                   │
│  ✓ 基础绘制函数封装                                             │
│  交付: 可移动的方块角色                                          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      Day 2: 工位系统                            │
├─────────────────────────────────────────────────────────────────┤
│  ✓ 4个工位显示 (色块 + 位置)                                    │
│  ✓ 工位交互 (点击触发)                                          │
│  ✓ 工位状态 (空闲/工作中/已完成)                                │
│  ✓ 进度条显示                                                   │
│  ✓ 简单的食物状态机                                             │
│  交付: 可点击的4个工作站                                         │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      Day 3: 制作流程                            │
├─────────────────────────────────────────────────────────────────┤
│  ✓ 烤肉 → 切肉 → 夹馍 → 完成 的完整流程                          │
│  ✓ 玩家手持物品状态                                              │
│  ✓ 食物放置到工位                                                │
│  ✓ 工位完成触发                                                  │
│  交付: 完整制作链可玩                                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      Day 4: 顾客系统                            │
├─────────────────────────────────────────────────────────────────┤
│  ✓ 顾客生成 (从右侧进入)                                        │
│  ✓ 顾客排队 (3个位置)                                           │
│  ✓ 简单订单显示 (需要的肉夹馍数量)                               │
│  ✓ 顾客等待计时                                                  │
│  ✓ 交付食物 → 顾客离开 + 金币                                   │
│  交付: 顾客出现到离开的完整流程                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      Day 5: 游戏循环                            │
├─────────────────────────────────────────────────────────────────┤
│  ✓ 金币系统                                                     │
│  ✓ 分数计算                                                     │
│  ✓ 基础升级系统 (烤肉速度 + 烤馍速度)                           │
│  ✓ 失败判定 (顾客耐心耗尽离开)                                  │
│  ✓ 游戏结束界面                                                  │
│  交付: 完整游戏循环可玩                                          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      Day 6: UI 完善                             │
├─────────────────────────────────────────────────────────────────┤
│  ✓ 金币/分数显示                                                 │
│  ✓ 升级菜单 (暂停时打开)                                        │
│  ✓ 开始/结束界面                                                │
│  ✓ 简单的开始按钮                                                │
│  交付: 可玩的完整游戏                                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      Day 7: 优化 + 测试                         │
├─────────────────────────────────────────────────────────────────┤
│  ✓ Bug 修复                                                    │
│  ✓ 数值平衡调整                                                  │
│  ✓ 打包发布 (Windows/macOS)                                    │
│  ✓ 简单自测                                                     │
│  交付: 可发布版本                                                │
└─────────────────────────────────────────────────────────────────┘
```

### 17.3 极度简化版 vs 完整版

| 功能 | 完整版 | 极度简化版 (一周) |
|------|--------|-------------------|
| **图形** | 精美PNG | 彩色矩形 |
| **音效** | BGM + SFX | 无 |
| **顾客** | 4种类型 | 1种类型 |
| **订单** | 随机多样 | 固定1个肉夹馍 |
| **工位** | 4个 | 4个 (保持核心玩法) |
| **升级** | 6种×5级 | 2种 (烤肉/烤馍速度) |
| **存档** | 完整JSON | 无 |
| **动画** | 流畅动画 | 状态切换 |
| **顾客数** | 多个同时 | 最多3个排队 |

### 17.4 代码量估算 (极度简化版)

| 模块 | 预估代码量 | 说明 |
|------|-----------|------|
| 主循环 + 工具 | 100 行 | 基础框架 |
| 玩家移动 | 50 行 | 简单移动 |
| 工位系统 | 150 行 | 4个工位 |
| 食物状态 | 100 行 | 状态机 |
| 顾客系统 | 150 行 | 生成+排队+离开 |
| 订单+收益 | 50 行 | 简单计算 |
| 升级系统 | 80 行 | 2种升级 |
| UI 显示 | 80 行 | 文字+按钮 |
| **总计** | **~760 行** | 可行！ |

### 17.5 可以跳过的功能清单

| 功能 | 跳过方式 |
|------|----------|
| 存档 | 不保存，下次从头开始 |
| 音效 | 完全不实现 |
| 多种顾客 | 只用一种"普通顾客" |
| 复杂订单 | 永远只需要1个肉夹馍 |
| 多种升级 | 只有"烤肉快20%"和"烤馍快20%" |
| 动画 | 状态直接切换，无过渡 |
| 粒子特效 | 完全不实现 |
| 移动端支持 | 只支持桌面 |

### 17.6 关键设计决策

```cpp
// 极度简化版设计

// 顾客永远只点 1 个肉夹馍
struct Order {
    int roujiamoCount = 1;  // 永远是1
    bool wantBeverage = false;  // 没有饮料
};

// 顾客只有一种类型
enum class CustomerType { NORMAL };  // 只有普通

// 只有2种升级
enum class UpgradeType {
    GRILL_SPEED,   // 烤肉速度+
    OVEN_SPEED     // 烤馍速度+
};

// 工位保持4个，但逻辑简单
enum class StationType { GRILL, OVEN, CUTTING, ASSEMBLY };
```

### 17.7 视觉风格 (色块方案)

```
┌────────────────────────────────────────┐
│  金币: 100    分数: 50                 │  ← 顶部UI
├────────────────────────────────────────┤
│                                        │
│    [烤肉架] [烤馍炉] [砧板] [组装台]    │  ← 4个工作站
│       🔴         🟠        🔪       🥙    │
│                                        │
│         👨‍🍵                            │  ← 玩家(绿色方块)
│                                        │
│  👤👤👤                                │  ← 顾客排队(蓝色方块)
│   等待中...                            │
│                                        │
└────────────────────────────────────────┘
```

### 17.8 结论

**一周完全可行！** 只需要：
- 用彩色矩形代替图片
- 跳过音效
- 简化顾客和订单
- 接受"从零开始"无存档

---

**确认一下**: 我们就用这个极度简化版方案，一周冲刺？

---

## 18. 两人分工方案 (一周极限版)

### 18.1 分工原则

```
┌─────────────────────────────────────────────────────────────────┐
│                      最大化并行，减少等待                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   程序员A                          程序员B                      │
│  ┌─────────────┐               ┌─────────────┐                │
│  │   底层系统   │               │   游戏逻辑   │                │
│  │ (渲染/输入)  │◄── 共享接口 ──►│ (工位/顾客)  │                │
│  └─────────────┘               └─────────────┘                │
│                                                                 │
│   依赖: 无                           依赖: A的渲染接口           │
│   开发: 前2天                       开发: 前2天                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 18.2 模块依赖关系

```
                    ┌─────────────┐
                    │   main.cpp  │
                    │   (共享)    │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              ▼                         ▼
    ┌─────────────────┐       ┌─────────────────┐
    │   程序员A       │       │   程序员B       │
    │ ┌─────────────┐ │       │ ┌─────────────┐ │
    │ │   Renderer  │ │       │ │   GameState │ │
    │ │   Input     │ │       │ │   Chef      │ │
    │ │   UI Draw   │ │       │ │   Station   │ │
    │ └─────────────┘ │       │ │   Customer  │ │
    │ ┌─────────────┐ │       │ │   OrderSys  │ │
    │ │  Raylib 封装│ │       │ │   Upgrade   │ │
    │ └─────────────┘ │       │ └─────────────┘ │
    └─────────────────┘       └─────────────────┘
```

### 18.3 每日任务分配

#### Day 1: 基础框架

| 程序员 A | 程序员 B |
|----------|----------|
| CMake + Raylib 初始化 | 常量定义 (颜色/尺寸) |
| 游戏窗口创建 | Logger 工具 |
| 主循环框架 | 游戏状态枚举 |
| 交付: 空窗口可运行 | 交付: 定义好接口 |

#### Day 2: 核心系统

| 程序员 A | 程序员 B |
|----------|----------|
| Renderer 类 (DrawRect, DrawText) | Chef 玩家移动逻辑 |
| Input 类 (键盘/鼠标) | 4个工位数据结构 |
| 基础 UI 绘制函数 | 工位交互逻辑 |
| 交付: 渲染+输入接口 | 交付: 玩家可移动 |

**依赖**: B 需要等待 A 的 Input 类

---

#### Day 3: 游戏逻辑

| 程序员 A | 程序员 B |
|----------|----------|
| 进度条绘制 | 烤肉→切肉→夹馍状态机 |
| 状态文字绘制 | 玩家手持物品 |
| 工位视觉反馈 | 食物完成判定 |
| 交付: UI 绘制函数 | 交付: 制作流程可玩 |

**依赖**: 无，可完全并行

---

#### Day 4: 顾客系统

| 程序员 A | 程序员 B |
|----------|----------|
| 顾客绘制 (矩形) | 顾客生成逻辑 |
| 顾客移动动画 | 顾客排队逻辑 |
| 订单显示 UI | 顾客等待计时 |
| 交付: 顾客视觉 | 交付: 顾客完整行为 |

**依赖**: 无，可完全并行

---

#### Day 5: 游戏循环

| 程序员 A | 程序员 B |
|----------|----------|
| 金币/分数 UI | 收益计算逻辑 |
| 游戏结束画面 | 失败判定 |
| 开始/暂停菜单 | 升级系统 (2种) |
| 交付: 完整 UI | 交付: 游戏循环 |

**依赖**: B 需要用 A 的 UI 函数

---

#### Day 6 + 7: 整合与测试

| 程序员 A | 程序员 B |
|----------|----------|
| Bug 修复 | 数值平衡 |
| 打包发布 | 测试反馈 |
| 代码优化 | 文档整理 |

---

### 18.4 关键接口定义 (Day 1 必须确定)

```cpp
// ===== renderer.h (程序员A定义，B使用) =====

class Renderer {
public:
    void DrawRect(float x, float y, float w, float h, Color color);
    void DrawText(const char* text, float x, float y, int size, Color color);
    void DrawProgressBar(float x, float y, float w, float h, float progress, Color bg, Color fg);
};

// ===== input.h (程序员A定义，B使用) =====

class Input {
public:
    static bool IsKeyDown(int key);
    static bool IsKeyPressed(int key);
    static bool IsMouseButtonDown(int button);
    static Vector2 GetMousePosition();
};

// ===== gameobjects.h (程序员B定义，A使用) =====

struct Station {
    StationType type;
    float x, y, w, h;
    StationState state;  // IDLE, WORKING, DONE
    float progress;
};

struct Customer {
    float x, y;
    int patience;  // 剩余秒数
    Order order;
    bool hasFood;
};
```

### 18.5 交付物对照表

| 天 | 程序员 A 交付 | 程序员 B 交付 |
|----|---------------|---------------|
| Day 1 | 空窗口 + 主循环 | 常量 + 枚举定义 |
| Day 2 | Renderer + Input | Chef 移动 + 工位数据 |
| Day 3 | 进度条 + 状态文字 | 制作状态机 |
| Day 4 | 顾客绘制 + 订单UI | 顾客AI + 队列 |
| Day 5 | UI 界面 | 收益 + 升级 + 失败判定 |
| Day 6-7 | 整合测试 | 平衡调整 |

### 18.6 协作注意事项

1. **Day 1 必须确定接口**: A 和 B 第一个上午必须确定所有头文件
2. **每天下班前合并**: 避免最后整合出问题
3. **使用宏定义共享常量**: 确保颜色、尺寸一致
4. **有问题及时沟通**: 不要卡太久

### 18.7 风险与应对

| 风险 | 概率 | 解决方案 |
|------|------|----------|
| 等待 A 的接口 | 高 | Day 1 上午确定接口，B 用 mock |
| 合并冲突 | 中 | 每天小合并，早发现早解决 |
| Bug 太多 | 中 | Day 6-7 预留充足时间 |
| 数值不平衡 | 低 | B 负责，可在 Day 5 调整 |

---

### 18.8 总结

| 程序员 | 负责模块 | 代码量预估 |
|--------|----------|-----------|
| **A** | 渲染 + 输入 + UI绘制 | ~300 行 |
| **B** | 游戏逻辑 + 顾客 + 工位 + 升级 | ~460 行 |
| **合计** | | ~760 行 |

**关键**: Day 1 确定接口后，Day 2-5 可完全并行开发！

---

*AI 辅助估算版本: 1.0*
*最后更新: 2026-04-30*
